<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable name="profile" type="com.github.kr328.clash.service.model.Profile" />
        <variable name="currentTime" type="com.github.kr328.clash.design.ui.ObservableCurrentTime" />
        <variable name="clicked" type="android.view.View.OnClickListener" />
        <variable name="menu" type="android.view.View.OnClickListener" />
        <import type="android.view.View" />
        <import type="com.github.kr328.clash.design.util.I18nKt" />
        <import type="com.github.kr328.clash.design.util.IntervalKt" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/root_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="16dp"
        android:layout_marginVertical="6dp"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackground"
        android:minHeight="112dp"
        android:onClick="@{clicked}"
        app:cardBackgroundColor="@{profile.active ? @color/aurora_secondary_container : @color/aurora_surface_container}"
        app:cardCornerRadius="12dp"
        app:cardElevation="0dp"
        app:strokeWidth="0dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="16dp"
            android:paddingTop="16dp"
            android:paddingEnd="8dp"
            android:paddingBottom="14dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <FrameLayout
                    android:layout_width="42dp"
                    android:layout_height="42dp"
                    android:background="@drawable/bg_aurora_icon_container">

                    <ImageView
                        android:layout_width="24dp"
                        android:layout_height="24dp"
                        android:layout_gravity="center"
                        android:contentDescription="@null"
                        android:src="@drawable/ic_baseline_cloud_download"
                        android:tint="@color/aurora_on_secondary_container" />
                </FrameLayout>

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="@{profile.name}"
                        android:textAppearance="@style/TextAppearance.Aurora.TitleMedium"
                        android:textColor="@color/aurora_on_surface" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="2dp"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="@{profile.active ? @string/aurora_profile_active : (profile.pending ? @string/format_type_unsaved(I18nKt.toString(profile.type, context)) : I18nKt.toString(profile.type, context))}"
                        android:textAppearance="@style/TextAppearance.Aurora.BodyMedium"
                        android:textColor="@color/aurora_on_surface_variant" />
                </LinearLayout>

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_marginStart="8dp"
                    android:contentDescription="@string/aurora_profile_active"
                    android:src="@drawable/ic_outline_check_circle"
                    android:tint="@color/aurora_primary"
                    android:visibility="@{profile.active ? View.VISIBLE : View.GONE}" />

                <ImageButton
                    android:id="@+id/menu_view"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/more"
                    android:onClick="@{menu}"
                    android:padding="12dp"
                    android:src="@drawable/ic_baseline_more_vert"
                    android:tint="@color/aurora_on_surface_variant" />
            </LinearLayout>

            <ProgressBar
                style="?android:attr/progressBarStyleHorizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:layout_marginStart="54dp"
                android:layout_marginTop="14dp"
                android:layout_marginEnd="8dp"
                android:max="1000"
                android:progress="@{profile.total > 1 ? I18nKt.toProgress((profile.download + profile.upload) / (profile.total / 1000)) : 0}"
                android:progressBackgroundTint="@color/aurora_outline_variant"
                android:progressTint="@color/aurora_primary"
                android:visibility="@{profile.total &lt; 2 ? View.GONE : View.VISIBLE}" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="54dp"
                android:layout_marginTop="8dp"
                android:layout_marginEnd="8dp"
                android:gravity="center_vertical"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:text='@{profile.total > 1 ? I18nKt.toBytesString(profile.download + profile.upload) + "/" + I18nKt.toBytesString(profile.total) : (profile.expire > 0 ? I18nKt.toDateStr(profile.expire) : "")}'
                    android:textAppearance="@style/TextAppearance.Aurora.BodyMedium"
                    android:textColor="@color/aurora_on_surface_variant"
                    android:visibility="@{profile.total > 1 || profile.expire > 0 ? View.VISIBLE : View.INVISIBLE}" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:text="@{IntervalKt.elapsedIntervalString(currentTime.value - profile.updatedAt, context)}"
                    android:textAppearance="@style/TextAppearance.Aurora.BodyMedium"
                    android:textColor="@color/aurora_on_surface_variant" />
            </LinearLayout>
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>
